# 樂齡學習平台 — 手機版專案規格

> 給 VS Code Claude 的實作規格。**請先讀 `DESIGN_SYSTEM.md`**，所有顏色、字級、尺寸、元件都以那份為準，本文件不重複定義。
> 這是一個高齡友善的數位學習平台，使用者是 60 歲以上、對手機操作不熟、視力與精細動作退化的長輩。

---

## 專案目標與背景

這份設計來自對 6 位高齡使用者的 UX 訪談（KJ 分析）。歸納出的核心痛點直接對應設計決策：

| 訪談痛點 | 設計回應 |
|----------|----------|
| 怕按錯、按錯回不來 | 每頁有「回上一步」、送出可回復、避免深層巢狀 |
| 課程學了就忘 | 課程頁有影片 + 步驟、可收藏到「有興趣的課」 |
| 不敢問家人、習慣問人 | 「語音問問題」常駐每頁 |
| 偏好影片勝於文字 | 卡片標明影片/文章、首頁推影片 |
| 對平台沒歸屬感（內容跟我無關）| 進站可選地區，線下課顯示在地資訊 |
| 對新科技好奇但不知入口 | 「AI 入門」類別、難度標示、用日常語言 |

---

## 技術棧

- **React 18 + Vite**
- **Tailwind CSS**（設定見 DESIGN_SYSTEM.md，引用 CSS 變數）
- **Heroicons**（`@heroicons/react/24/outline`）
- **React Router**（四頁切換）
- 狀態：先用 React `useState` / Context 即可，不接後端（用假資料）
- **不要**用 localStorage（先放記憶體狀態）

### 專案結構建議

```
src/
  styles/
    tokens.css          # DESIGN_SYSTEM.md 的 CSS 變數區塊，全域 import
  components/
    TabBar.jsx          # 底部四個 Tab（每頁共用）
    VoiceFab.jsx        # 語音問答浮動鈕（每頁共用）
    TopBar.jsx          # 頂部標題列 + 回上一步
    CourseCard.jsx      # 課程卡片
    Tag.jsx             # 標籤（影片/線上/線下/類別）
    Chip.jsx            # 分類晶片
    SearchField.jsx     # 搜尋欄
  pages/
    HomePage.jsx        # 首頁
    FindCoursePage.jsx  # 找課程
    SavedPage.jsx       # 有興趣的課
    VoiceAskPage.jsx    # 語音問答
  data/
    courses.js          # 假課程資料
  App.jsx               # Router + 共用 layout（TabBar/Fab 固定）
  main.jsx
```

### 共用 Layout 規則

`App.jsx` 包一個固定 layout：內容區可捲動，**TabBar 永遠固定底部、VoiceFab 永遠固定右下**，四頁切換時這兩個不動（位置一致，WCAG 3.2.3）。

```
┌─────────────────┐
│  TopBar（標題）  │  ← 各頁不同，含回上一步
├─────────────────┤
│                 │
│   頁面內容        │  ← 可捲動
│   （可捲動）      │
│           [FAB] │  ← 語音問答，固定
├─────────────────┤
│ 🏠 🔍 ⭐ 🎤      │  ← TabBar，固定
└─────────────────┘
```

---

## 頁面 1：首頁 HomePage

由上到下：

1. **TopBar**：顯示「樂齡學習平台」，首頁不需要回上一步。
2. **最新消息輪播**：單張寬卡 + 下方圓點指示。**可手動左右滑，不自動播放**（2.2.2 避免自動移動內容）。每張：類型標籤 + 標題。
3. **搜尋欄**：placeholder「搜尋課程⋯⋯」，點了跳到找課程頁。
4. **學習類別格狀**：3 欄 grid，每格 = 圓形圖示 + 類別名。點了跳找課程頁並帶入該類別 filter。類別：手機操作、AI 入門、網路購物、健保查詢、拍照修圖、影音娛樂、網路銀行、更多。

每格 `min-height: 88px`，圖示 32px，整格可點（觸控面積大）。

---

## 頁面 2：找課程 FindCoursePage（核心頁）

由上到下：

1. **TopBar**：標題「找課程」+ 回上一步 + 右側「共 N 堂」。
2. **搜尋欄**。
3. **上課方式 toggle**：全部 / 線上 / 線下（三段，pill 樣式，選取藍底白字）。這是長輩**第一個要分辨的事**（要不要出門），放最上面。
4. **類別 chips**：橫向可滑。**注意**：先前 wireframe 出現 chips 被卡片遮住的 bug，請確保 chips 區塊有獨立高度、`overflow-x: auto`、不被下方 absolute 元素覆蓋。
5. **課程卡片列表**：單欄。每張卡片用 `CourseCard`。

### CourseCard 結構

```
┌──────────────────────┐
│   [▶] 影片縮圖         │ ← 縮圖區 120px
├──────────────────────┤
│ [影片][線上][手機操作]  │ ← 標籤列（圖示+文字）
│ LINE 視訊通話完整教學   │ ← 標題 body-lg 粗體
│ 從加好友到視訊，一步步… │ ← 描述 body / text-secondary
│ ⏱18分鐘 · 👁3,241人   │ ← meta caption / text-muted（線上）
│ 📍台南東區 · 📅06/28  │ ← 線下則顯示地點+日期
└──────────────────────┘
[♡ 加入有興趣]  ← 收藏鈕，點了加入 SavedPage
```

- 線上課顯示：時長 + 觀看數
- 線下課顯示：地點 + 日期（對應「偏好近距離」訪談）
- 收藏鈕點擊：加入「有興趣的課」+ 顯示成功回饋（3.3.4）

### 假資料欄位（data/courses.js）

```js
export const courses = [
  {
    id: 1,
    title: 'LINE 視訊通話完整教學',
    desc: '從加好友、撥打視訊到螢幕分享，一步一步帶你學會跟家人視訊。',
    type: 'video',        // 'video' | 'article'
    mode: 'online',       // 'online' | 'offline'
    category: 'phone',    // phone | ai | shopping | health | photo ...
    duration: 18,         // 分鐘（online）
    views: 3241,          // online
    location: null,       // offline 才有，例 '台南市 東區'
    date: null,           // offline 才有，例 '06/28 週六 09:00'
    thumbnail: null,      // 先 null，用 placeholder
  },
  // …再多塞 5-6 筆，線上線下混合
];
```

---

## 頁面 3：有興趣的課 SavedPage

兩個區塊（用 section 分隔，各有 h2 標題）：

1. **已報名**：顯示報名成功的線下課，**含確認紀錄**（報名時間、地點、日期）。這直接回應訪談「希望看到報名成功的確認紀錄」。
2. **收藏的課**：使用者按過愛心的課程，沿用 CourseCard。

空狀態：若沒有收藏，顯示友善引導「還沒有收藏的課，去『找課程』看看吧」+ 一個跳轉按鈕（空狀態是行動引導，不是死路）。

---

## 頁面 4：語音問答 VoiceAskPage

這頁也是 FAB 點下去會到的地方。設計重點是**降低開口障礙**（訪談：不敢問人）。

1. **大麥克風按鈕**：置中、大尺寸（至少 96px 圓），點了開始錄音（先做 UI 狀態即可：閒置 / 錄音中 / 處理中）。按鈕下方文字「點一下，說出你的問題」。
2. **常見問題快捷**：下方列幾個常見問題按鈕（如「怎麼用 LINE 視訊？」「健保卡怎麼查？」），點了直接帶入問題。長輩不一定想得到要問什麼，給範例降低門檻。
3. **對話區**：問答以氣泡顯示，AI 回答用**日常語言、附操作步驟**（3.1.5）。回答若引用課程，附上課程卡片連結。

> 語音辨識可先用瀏覽器 Web Speech API（`window.SpeechRecognition`）做 demo，設定 `lang = 'zh-TW'`。先不接真的 LLM，回答可先寫死範例。

---

## 無障礙實作檢查清單（交付前自測）

實作完成後，請逐項確認：

- [ ] 用鍵盤 Tab 能走過所有按鈕、連結、輸入框，焦點環清楚可見（3px）
- [ ] 瀏覽器縮放到 200%，版面不破、不出現橫向捲軸（1.4.4）
- [ ] 所有可點元件實測 ≥ 48px 高
- [ ] 每個圖示按鈕都有 `aria-label`；裝飾性圖示加 `aria-hidden`
- [ ] 用語義標籤：`<nav>` `<main>` `<button>` `<h1~h3>`，不要用 `<div onClick>` 當按鈕
- [ ] 表單欄位有 `<label>`（3.3.2）
- [ ] 狀態不只靠顏色：選取有框、標籤有圖示文字（1.4.1）
- [ ] 輪播不自動播放、可手動控制（2.2.2）
- [ ] TabBar 在四頁位置完全一致（3.2.3）
- [ ] 收藏/報名動作有明確成功回饋（3.3.4）
- [ ] 文字用日常語言，不出現「prompt」「咒語」等術語（3.1.3）
- [ ] 圖片有 `alt`、影片預留字幕欄位（1.1.1 / 1.2.2）

---

## 實作順序建議

1. 先設定 `tokens.css` + `tailwind.config.js`（從 DESIGN_SYSTEM.md 複製）
2. 做共用元件：TabBar、VoiceFab、TopBar、Tag、Chip、CourseCard
3. 用假資料做「找課程」頁（最核心）
4. 再做首頁、有興趣的課、語音問答
5. 最後跑無障礙檢查清單

---

## 重要提醒

- **基準字級 18px 是刻意的**，不是 bug。長輩看得清楚優先於塞更多內容。
- **米白底 #FBF9F4 不是髒掉**，是刻意避免純白眩光（W3C 技術建議）。
- 寧可內容少、要捲動，也不要把字縮小、把按鈕擠小。
- 拿不準的尺寸/顏色，一律回 DESIGN_SYSTEM.md 查，不要自己發明。
