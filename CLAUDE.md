# 專案規則（每次動作前必讀）

## Design System 來源

- 唯一依據是 DESIGN_SYSTEM.md，數值一律照它，禁止自創顏色/尺寸
- 那些數值對應 W3C WAI / WCAG 準則，要改先回頭看對應準則

## 改動紀律

- 只改我當次明確指定的檔案
- 不重構、不優化、不碰沒指定的部分
- 尚未套 design system 的舊元件維持原樣
- 每次做完列出改動的檔案清單，等我確認再繼續

## 無障礙底線（不可違反）

- 字級基準 18px、全用 rem
- 觸控目標 ≥ 48px
- 焦點環 3px、互動狀態不只靠顏色（要加框或圖示）
